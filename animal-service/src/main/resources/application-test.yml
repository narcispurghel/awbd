spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  cloud:
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
cors:
  allowed-origins: http://localhost:8090
